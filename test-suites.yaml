version: "3"
services:
  runTest:
    image: phuocleautoqa/selenium
    environment:
      - BROWSER
      - HUB_HOST=hub
      - THREAD_COUNT
      - TEST_SUITE
    volumes:
      - /home/selenium-docker/test-output
